load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "apu",
    srcs = [
        "apu.cc",
    ],
    hdrs = [
        "apu.h",
    ],
    visibility = ["//visibility:public"],
    deps = [
    ],
)
